'use strict';

var expect = require('chai').expect;

describe('Script', function () {
    describe('foo', function () {
        it('should', function () {
            expect(true).to.be(true);
        });
    });
});
